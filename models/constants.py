"""Shared constants for models and core layers."""

# Default tolerance for die CLR matching (in display units).
# Used when comparing detected CLR against die specifications.
DIE_CLR_MATCH_DEFAULT: float = 0.01
